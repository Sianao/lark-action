name: build 

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: Integration failure notifications
        uses: sianao/lark-action@${{ github.sha }} 
        with:
          state: "hello world"
   


